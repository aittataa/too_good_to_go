flutter pub get
flutter pub run flutter_launcher_icons:main

------------------Build APK------------------------------------------------------------------------------------------------
flutter clean
flutter build apk --release
flutter build apk --release --target-platform android-arm
flutter build apk --release --target-platform android-arm --analyze-size

--------------------------------
/*
SizedBox(
  child: Column(
    mainAxisAlignment: MainAxisAlignment.spaceAround,
    children: [
      ExpandedLogo(),
      ListTile(
        title: SubtitleText(
          subtitle: "The data the app collect includes your log in details and order history",
        ),
      ),
      ListTile(
        title: SubtitleText(
          subtitle: "Let us know and we will send you a report containing a copy of this data via email.",
        ),
      ),
      ListTile(
        title: ButtonClick(
          onPressed: () => showDialog(context: context, builder: (_) => MessageBox()),
          title: "Request My Data".toUpperCase(),
          textColor: AppTheme.whiteTextColor,
          backColor: AppTheme.mainColor,
        ),
      ),
    ],
  ),
),

*/

/*
Container(
  decoration: BoxDecoration(
    color: AppTheme.whiteBackColor,
    borderRadius: BorderRadius.circular(25),
    boxShadow: [
      BoxShadow(
        color: Colors.black12,
        spreadRadius: -1,
        blurRadius: 10,
      ),
    ],
  ),
  height: 500,
  child: Column(
    children: [
      Expanded(
        child: Container(
          width: double.infinity,
          decoration: BoxDecoration(
            color: Colors.black12,
            borderRadius: BorderRadius.only(
              topLeft: Radius.circular(25),
              topRight: Radius.circular(25),
            ),
          ),
          child: Image.asset(Messages.LOGO_ICON),
        ),
      ),
      Expanded(
        child: Container(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              ListTile(
                subtitle: Text(
                  "TIPS & TRICKS",
                  style: TextStyle(
                    color: AppTheme.lightMainColor,
                    fontWeight: FontWeight.bold,
                    fontSize: 20,
                  ),
                ),
              ),
              ListTile(
                title: Text(
                  "How and when to wash your fresh produce".toUpperCase(),
                  style: TextStyle(
                    color: AppTheme.mainColor,
                    fontWeight: FontWeight.w900,
                    fontSize: 30,
                  ),
                ),
              ),
              ListTile(
                subtitle: Text(
                  "17 hours ago | Sarah Hardit",
                  style: TextStyle(
                    color: AppTheme.mainColor,
                    fontWeight: FontWeight.bold,
                  ),
                ),
              ),
            ],
          ),
        ),
      ),
    ],
  ),
),
SizedBox(height: 15),
Container(
  decoration: BoxDecoration(
    color: AppTheme.whiteBackColor,
    borderRadius: BorderRadius.circular(25),
    boxShadow: [
      BoxShadow(
        color: Colors.black12,
        spreadRadius: -1,
        blurRadius: 10,
      ),
    ],
  ),
  height: 500,
  child: Column(
    children: [
      Expanded(
        child: Container(
          width: double.infinity,
          decoration: BoxDecoration(
            color: Colors.black12,
            borderRadius: BorderRadius.only(
              topLeft: Radius.circular(25),
              topRight: Radius.circular(25),
            ),
          ),
          child: Image.asset(Messages.LOGO_ICON),
        ),
      ),
      Expanded(
        child: Container(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              ListTile(
                subtitle: Text(
                  "TIPS & TRICKS",
                  style: TextStyle(
                    color: AppTheme.lightMainColor,
                    fontWeight: FontWeight.bold,
                    fontSize: 20,
                  ),
                ),
              ),
              ListTile(
                title: Text(
                  "How and when to wash your fresh produce".toUpperCase(),
                  style: TextStyle(
                    color: AppTheme.mainColor,
                    fontWeight: FontWeight.w900,
                    fontSize: 30,
                  ),
                ),
              ),
              ListTile(
                subtitle: Text(
                  "17 hours ago | Sarah Hardit",
                  style: TextStyle(
                    color: AppTheme.mainColor,
                    fontWeight: FontWeight.bold,
                  ),
                ),
              ),
            ],
          ),
        ),
      ),
    ],
  ),
),
SizedBox(height: 15),
*/

